<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qichong.dao.GenderDao">

	<!-- 分页查询出所有性别 -->
	<select id="selectAll" resultType="Gender">
		SELECT * FROM gender_dao
		LIMIT #{limit},#{offset}
	</select>


</mapper>